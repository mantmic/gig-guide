name: CI

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.8]
    steps:
    - uses: actions/checkout@v1
    - name: Install python modules
      run: |
        python -m pip install --upgrade pip
        cd extract
        pip install -r requirements.txt
    - name: Install dbt
      run: |
        pip install dbt
    - name: Run extract scripts
      run: |
        cd extract
        python get_datamelbourne.py
    - name: Run DBT
      run: |
        cd sql
        dbt run --target prod
